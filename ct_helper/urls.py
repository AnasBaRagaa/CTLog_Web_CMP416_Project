from django.urls import path
from . import views

app_name = 'ct_helper'
urlpatterns = [
    # path('', views.IndexView.as_view(), name='index'),
    # path('hospitals/', views.DetailView.as_view(), name='hospitals'),
    # path('hospitals/add/', views.DetailView.as_view(), name='hospitals'),
    # path('patients/', views.CreateEntry.as_view(), name='patients'),
    # path('surgeons/', views.DetailView.as_view(), name='surgeons'),
    # path('operations/', views.CreateEntry.as_view(), name='operations'),
    # path('drugs/', views.DetailView.as_view(), name='drugs'),
    # path('tests/<int:operation>/', views.CreateEntry.as_view(), name='create'),
]
